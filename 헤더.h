#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <windows.h>
#pragma warning(disable:4996)

void my_grade();
void put_data();
int timesum(int, int);
float GPAsum(int, int, float);
void grade_manage();
void grade_check(int, int);
